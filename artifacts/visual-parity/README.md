# Visual Parity Artifacts

This folder is for local-only visual parity screenshot outputs.

`mise run visual-parity:capture` writes captures and `report.md` under:

```text
artifacts/visual-parity/latest
```

All generated files are ignored by git. Only this README and `.gitignore` are tracked.
