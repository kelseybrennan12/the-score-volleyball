#!/usr/bin/env bash
#MISE description="Execute the default automated unit/contract suite"
#MISE depends=["test:image:prepare"]

set -euo pipefail

cd "${MISE_PROJECT_ROOT:?}"

if [[ "${1:-}" == "--" ]]; then
  shift
fi

if [[ "$#" -gt 0 ]]; then
  mise run test -- "$@"
else
  mise run test
fi
