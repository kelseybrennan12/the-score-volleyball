#!/usr/bin/env bash
#MISE description="Run local CI-equivalent checks through the canonical task graph"
#MISE depends=["fmt-check", "deps:check", "lint", "build", "test:unit", "test:integration", "test:e2e"]

set -euo pipefail

cd "${MISE_PROJECT_ROOT:?}"
printf 'local ci completed successfully\n'
